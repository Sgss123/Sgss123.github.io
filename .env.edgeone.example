# EdgeOne Makers 环境变量参考
#
# EdgeOne Makers 通过 Git 集成自动构建部署，环境变量请在控制台配置：
#   EdgeOne 控制台 -> Makers -> 项目设置 -> Environment Variables
#
# 本文件仅作为项目所需环境变量的清单参考，不需要提交实际值。
# 本地调试（edgeone pages dev）时，可复制为 .env.edgeone 使用。

# 应用部署环境：production | preview
DEPLOY_ENV=production

# 应用运行时配置示例（按需添加）
# APP_BASE_URL=https://www.waterspo.top
